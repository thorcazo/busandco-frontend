import { NombrelineasPipe } from './nombrelineas.pipe';

describe('NombrelineasPipe', () => {
  it('create an instance', () => {
    const pipe = new NombrelineasPipe();
    expect(pipe).toBeTruthy();
  });
});
