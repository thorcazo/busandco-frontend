import { EmpresalineasPipe } from './empresalineas.pipe';

describe('EmpresalineasPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpresalineasPipe();
    expect(pipe).toBeTruthy();
  });
});
