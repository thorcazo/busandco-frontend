import { TipolineasPipe } from './tipolineas.pipe';

describe('TipolineasPipe', () => {
  it('create an instance', () => {
    const pipe = new TipolineasPipe();
    expect(pipe).toBeTruthy();
  });
});
